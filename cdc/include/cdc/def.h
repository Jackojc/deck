#ifndef CDC_DEF_H
#define CDC_DEF_H

#include <stdbool.h>

// Typedefs
typedef int dk_err_t;             // Used for error handling
typedef bool (*dk_pred_t)(char);  // Used for lexer predicates

#endif
